/*
 * TypesAndMacros.h
 *
 *  Created on: Jul 16, 2014
 *      Author: jonasbachli
 */

#ifndef TYPESANDMACROS_H_
#define TYPESANDMACROS_H_

#include <stdint.h>
#include <stdbool.h>

#ifndef NULL
#define NULL (void*)0
#endif

#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

#endif /* TYPESANDMACROS_H_ */
